Make sure you have copied the JNI include files that come with the JDK to include/
Make sure you have built the C++ RAMCloud project
Use ./gradlew build to build the Java bindings
Make sure you have built the C++ RAMCloud test library with ./buildtest.sh when running unit tests
Use ./gradlew test to run the unit tests

Java and C++ sources for the bindings are located in src/
Java and C++ sources for the unit tests are located in testsrc/

Javadoc is available at: https://ramcloud.stanford.edu/docs/javadoc/